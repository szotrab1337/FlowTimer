<Window
    x:Class="FlowTimer.Wpf.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:c="clr-namespace:NKristek.Wpf.Converters;assembly=NKristek.Wpf.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="clr-namespace:FlowTimer.Wpf.Views"
    Title="Flow Timer"
    Width="1200"
    Height="800"
    d:DataContext="{d:DesignInstance views:MainWindow,
                                     IsDesignTimeCreatable=False}"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Window.Resources>

        <Style
            x:Key="TitleBarDefaultButtonStyle"
            TargetType="Button">
            <Setter Property="MinWidth" Value="48" />
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
            <Setter Property="VerticalAlignment" Value="Top" />
            <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            x:Name="EnclosingBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                            <ContentPresenter
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                RecognizesAccessKey="True"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ControlAltFillColorQuarternaryBrush}" />
                </Trigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding Path=(SystemParameters.HighContrast)}" Value="True" />
                        <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Mode=Self}}" Value="True" />
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="{DynamicResource SystemColorHighlightColorBrush}" />
                    <Setter Property="Foreground" Value="{DynamicResource SystemColorHighlightTextColorBrush}" />
                </MultiDataTrigger>
            </Style.Triggers>
        </Style>

        <Style
            x:Key="TitleBarDefaultCloseButtonStyle"
            BasedOn="{StaticResource TitleBarDefaultButtonStyle}"
            TargetType="Button">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C42B1C" />
                    <Setter Property="Foreground" Value="White" />
                </Trigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding Path=(SystemParameters.HighContrast)}" Value="True" />
                        <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Mode=Self}}" Value="True" />
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="{DynamicResource SystemColorHighlightColorBrush}" />
                    <Setter Property="Foreground" Value="{DynamicResource SystemColorHighlightTextColorBrush}" />
                </MultiDataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Border
        x:Name="HighContrastBorder"
        BorderBrush="Transparent"
        BorderThickness="8,1,8,8">
        <Grid x:Name="MainGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="44" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid
                Grid.Row="0"
                Grid.ColumnSpan="2"
                Height="44">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel
                    Grid.Column="0"
                    Margin="15,0,0,0"
                    Orientation="Horizontal">
                    <Image
                        Width="20"
                        VerticalAlignment="Center"
                        Source="{StaticResource AppIcon}" />
                    <TextBlock
                        Margin="16,0,0,0"
                        VerticalAlignment="Center"
                        AutomationProperties.HeadingLevel="Level1"
                        Style="{DynamicResource CaptionTextBlockStyle}"
                        Text="Flow Timer" />
                </StackPanel>

                <Button
                    x:Name="MinimizeButton"
                    Grid.Column="1"
                    Click="MinimizeWindow"
                    Style="{StaticResource TitleBarDefaultButtonStyle}">
                    <TextBlock
                        VerticalAlignment="Center"
                        FontFamily="{DynamicResource SymbolThemeFontFamily}"
                        FontSize="10"
                        Text="&#xE921;" />
                </Button>

                <Button
                    x:Name="MaximizeButton"
                    Grid.Column="2"
                    Click="MaximizeWindow"
                    Style="{StaticResource TitleBarDefaultButtonStyle}">
                    <TextBlock
                        x:Name="MaximizeIcon"
                        VerticalAlignment="Center"
                        FontFamily="{DynamicResource SymbolThemeFontFamily}"
                        FontSize="10"
                        Text="&#xE922;" />
                </Button>

                <Button
                    x:Name="CloseButton"
                    Grid.Column="3"
                    Click="CloseWindow"
                    Style="{StaticResource TitleBarDefaultCloseButtonStyle}">
                    <TextBlock
                        VerticalAlignment="Center"
                        FontFamily="{DynamicResource SymbolThemeFontFamily}"
                        FontSize="16"
                        Text="&#xE711;" />
                </Button>
            </Grid>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TreeView
                        x:Name="Menu"
                        Grid.Row="1"
                        Margin="8,8,0,0"
                        AutomationProperties.Name="Navigation Pane"
                        ItemsSource="{Binding ViewModel.MenuItems}"
                        PreviewMouseLeftButtonUp="Menu_PreviewMouseLeftButtonUp"
                        SelectedItemChanged="Menu_OnSelectedItemChanged">
                        <TreeView.ItemTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding Items}">
                                <Grid MinHeight="30">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="16" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock
                                        MaxWidth="16"
                                        VerticalAlignment="Center"
                                        AutomationProperties.Name="{Binding Title, StringFormat='{}{0} Page'}"
                                        Focusable="False"
                                        FontFamily="{DynamicResource SymbolThemeFontFamily}"
                                        FontSize="16"
                                        Text="{Binding Icon}" />
                                    <TextBlock
                                        Grid.Column="2"
                                        VerticalAlignment="Center"
                                        Text="{Binding Title}" />
                                </Grid>
                            </HierarchicalDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>

                    <Grid Grid.Row="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Border
                            x:Name="ActiveSessionCard"
                            Grid.Row="0"
                            Margin="10,10,3,5"
                            Padding="12"
                            Background="{DynamicResource SmokeFillColorDefaultBrush}"
                            BorderBrush="{DynamicResource ControlStrokeColorSecondaryBrush}"
                            BorderThickness="1"
                            CornerRadius="6"
                            Visibility="{Binding ViewModel.IsTimerRunning, Converter={c:BoolToVisibilityConverter}}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBlock
                                    Grid.Row="0"
                                    FontSize="11"
                                    FontWeight="SemiBold"
                                    Text="AKTYWNA SESJA" />

                                <TextBlock
                                    Grid.Row="1"
                                    Margin="0,6,0,0"
                                    FontSize="20"
                                    FontWeight="Bold"
                                    Text="{Binding ViewModel.ActiveSessionTime}" />

                                <StackPanel
                                    Grid.Row="2"
                                    Margin="0,8,0,0"
                                    Orientation="Horizontal">
                                    <Button
                                        Padding="8,4"
                                        Command="{Binding ViewModel.GoToActiveProjectCommand}">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock
                                                Margin="0,2,0,0"
                                                FontFamily="{DynamicResource SymbolThemeFontFamily}"
                                                FontSize="12"
                                                Text="&#xE8A7;" />
                                            <TextBlock
                                                Margin="6,0,0,0"
                                                FontSize="12"
                                                Text="Szczegóły" />
                                        </StackPanel>
                                    </Button>

                                    <Button
                                        Margin="8,0,0,0"
                                        Padding="8,4"
                                        Command="{Binding ViewModel.StopTimerCommand}">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock
                                                Margin="0,2,0,0"
                                                FontFamily="{DynamicResource SymbolThemeFontFamily}"
                                                FontSize="12"
                                                Text="&#xE71A;" />
                                            <TextBlock
                                                Margin="6,0,0,0"
                                                FontSize="12"
                                                Text="Zatrzymaj" />
                                        </StackPanel>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <Button
                            Grid.Row="1"
                            Width="250"
                            Margin="10,5,3,10"
                            HorizontalContentAlignment="Left"
                            AutomationProperties.Name="Settings"
                            Command="{Binding ViewModel.SettingsCommand}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock
                                    Margin="0,4,0,0"
                                    FontFamily="{DynamicResource SymbolThemeFontFamily}">
                                    &#xE713;
                                </TextBlock>
                                <TextBlock
                                    Margin="8,0,0,0"
                                    Text="Ustawienia" />
                            </StackPanel>
                        </Button>
                    </Grid>
                </Grid>
                <Border
                    Grid.Column="1"
                    Margin="4,0,0,0"
                    Padding="24,16,24,24"
                    Background="{DynamicResource LayerFillColorDefaultBrush}"
                    BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8,0,0,0">
                    <Frame
                        x:Name="RootContentFrame"
                        NavigationUIVisibility="Hidden" />
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
