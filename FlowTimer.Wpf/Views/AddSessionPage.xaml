<Page
    x:Class="FlowTimer.Wpf.Views.AddSessionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:FlowTimer.Wpf.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:FlowTimer.Wpf.ViewModels"
    Title="AddSessionPage"
    d:DataContext="{d:DesignInstance Type=viewModels:AddSessionViewModel,
                                     IsDesignTimeCreatable=True}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Grid.Column="0"
            VerticalAlignment="Center"
            Style="{DynamicResource TitleTextBlockStyle}"
            Text="Nowe sesja" />

        <Grid
            Grid.Row="1"
            Grid.Column="0"
            Margin="0,20,0,0">
            <ScrollViewer>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel
                        Grid.Row="0"
                        Grid.Column="0"
                        Margin="0,0,5,15">
                        <Label
                            Content="Od"
                            Focusable="True"
                            Style="{StaticResource GenericLabelStyle}" />
                        <controls:DateTimePicker
                            Margin="0,5,0,0"
                            SelectedDateTime="{Binding StartTime, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>

                    <StackPanel
                        Grid.Row="0"
                        Grid.Column="1"
                        Margin="5,0,0,15">
                        <Label
                            Content="Do"
                            Style="{StaticResource GenericLabelStyle}" />
                        <controls:DateTimePicker
                            Margin="0,5,0,0"
                            SelectedDateTime="{Binding EndTime, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>

                    <StackPanel
                        Grid.Row="1"
                        Grid.Column="0"
                        Grid.ColumnSpan="2"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Bottom"
                        Orientation="Horizontal">
                        <Button
                            Margin="0,0,10,0"
                            Command="{Binding CancelCommand}"
                            Content="Anuluj" />
                        <Button
                            Command="{Binding SaveCommand}"
                            Content="Zapisz"
                            Style="{DynamicResource AccentButtonStyle}" />
                    </StackPanel>
                </Grid>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
