<views:BaseWindow
    x:Class="FlowTimer.Wpf.Views.CompactTimerWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:FlowTimer.Wpf.ViewModels"
    xmlns:views="clr-namespace:FlowTimer.Wpf.Views"
    Title="Flow Timer"
    Width="300"
    Height="125"
    d:DataContext="{d:DesignInstance viewModels:CompactTimerViewModel,
                                     IsDesignTimeCreatable=False}"
    Closing="Window_OnClosing"
    Deactivated="Window_Deactivated"
    Loaded="Window_OnLoaded"
    ResizeMode="NoResize"
    mc:Ignorable="d">

    <Border
        x:Name="HighContrastBorder"
        BorderBrush="Transparent"
        BorderThickness="8,1,8,8">
        <Grid x:Name="MainGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="44" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid
                Grid.Row="0"
                Grid.ColumnSpan="2"
                Height="44">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel
                    Grid.Column="0"
                    Margin="15,0,0,0"
                    Orientation="Horizontal">
                    <Image
                        Width="20"
                        VerticalAlignment="Center"
                        Source="{StaticResource AppIcon}" />
                    <TextBlock
                        Margin="16,0,0,0"
                        VerticalAlignment="Center"
                        AutomationProperties.HeadingLevel="Level1"
                        Style="{DynamicResource CaptionTextBlockStyle}"
                        Text="Flow Timer" />
                </StackPanel>

                <Button
                    x:Name="CloseButton"
                    Grid.Column="1"
                    Click="CloseWindow"
                    Style="{StaticResource TitleBarDefaultCloseButtonStyle}">
                    <TextBlock
                        VerticalAlignment="Center"
                        FontFamily="{DynamicResource SymbolThemeFontFamily}"
                        FontSize="16"
                        Text="&#xE711;" />
                </Button>
            </Grid>

            <Grid
                Grid.Row="1"
                Margin="15,0,15,15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid
                    Grid.Row="0"
                    VerticalAlignment="Top">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock
                        Grid.Row="0"
                        Grid.Column="0"
                        VerticalAlignment="Center"
                        FontSize="11"
                        FontWeight="SemiBold"
                        Text="AKTYWNA SESJA" />

                    <Button
                        Grid.Column="1"
                        Margin="8,0,0,0"
                        Padding="8,4"
                        Click="StopTimer_Click"
                        Command="{Binding StopTimerCommand}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock
                                Margin="0,2,0,0"
                                FontFamily="{DynamicResource SymbolThemeFontFamily}"
                                FontSize="12"
                                Text="&#xE71A;" />
                            <TextBlock
                                Margin="6,0,0,0"
                                FontSize="12"
                                Text="Zatrzymaj" />
                        </StackPanel>
                    </Button>
                </Grid>

                <TextBlock
                    Grid.Row="1"
                    Margin="0,6,0,0"
                    FontSize="20"
                    FontWeight="Bold"
                    Text="{Binding ActiveSessionTime}" />

            </Grid>
        </Grid>
    </Border>
</views:BaseWindow>
